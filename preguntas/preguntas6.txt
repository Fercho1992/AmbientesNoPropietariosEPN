1.En caso de ser true va a ser 1 y si es false va a se 0
2.Para forzar la presedencia de operadores debemos de usar parentesis.
3.
4.El operador de identidad (===) lo podemos usar para comparar Strings por ejemplo
La variable $a y la varibale $b son comparados como strings.

if($a===$b)

5.continue es muy similiar al break pero con la diferencia que el Break termina el ciclo y el Continue
pasa a la siguiente iteraccion.

6. El operador ternario es ?x: y, funciona de forma similar a un if, de una manera mas
simplificada.
